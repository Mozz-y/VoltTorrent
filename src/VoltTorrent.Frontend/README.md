# VoltTorrent Frontend

This directory will contain the React + TypeScript frontend once the Photino walking skeleton work begins.

Planned stack:

- React
- TypeScript
- Vite
- Tailwind CSS
- Radix UI

The frontend should communicate with the .NET host through explicit IPC contracts owned at the app boundary.
